<span id="s-unranked" hx-swap-oob="true">{{ stats.unranked }}</span>
<span id="s-shortlist" hx-swap-oob="true">{{ stats.counts.shortlist or 0 }}</span>
<span id="s-applied" hx-swap-oob="true">{{ stats.counts.applied or 0 }}</span>
<span id="s-interview" hx-swap-oob="true">{{ stats.counts.interview or 0 }}</span>
<span id="s-offer" hx-swap-oob="true">{{ stats.counts.offer or 0 }}</span>
